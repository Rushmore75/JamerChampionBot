The program is expecting a database named `java_cloud_db` and a table with this:


```sql
create table users (
    UserID char(25),
    WINS int,
    LOSSES int,
    WINS_STREAK int,
    LOSSES_STREAK int
);
```

